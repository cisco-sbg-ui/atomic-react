---
name: Field Base
route: /extend/field-base
components: AFieldBase
---

import {Playground, Props} from "../../../docs";

# Field Base Component

## Install

Integrate with your build to [auto-import](?path=/docs/getting-started--page#integrating), or add an import in your component:

```
import AFieldBase from "@cisco-ats/atomic-react/AFieldBase";
// or if you're able to treekshake node_modules
import {AFieldBase} from "@cisco-ats/atomic-react";
```

## Usage

<Playground
  code={`<div style={{maxWidth: 300}}>
  <AFieldBase
    className="mb-3"
    label="Label - Normal"
    hint="This is a sample of hint text below a form field">
    [Children]
  </AFieldBase>
  <AFieldBase
    className="mb-3"
    label="Label - Warning"
    hint="This is a sample of hint text below a form field"
    validationState="warning">
    [Children]
  </AFieldBase>
  <AFieldBase
    className="mb-3"
    label="Label - Danger"
    hint="This is a sample of hint text below a form field"
    validationState="danger">
    [Children]
  </AFieldBase>
</div>
`}
/>

## Field Base Props

The `AFieldBase` component inherits passed props.

<Props of="AFieldBase" />

import { Meta, Preview, Story, Props } from '@storybook/addon-docs/blocks';
import {useState} from 'react'

import LoremIpsum from '../../utils/lorem-ipsum'
import YourComponent from '../../utils/storybook-function'
import ATextInput from "./ATextInput"

<Meta title="Components/Text Inputs" component={ATextInput} />

# Text Inputs

## Install

The component is available on [Cisco's Artifactory package registry](https://devhub.cisco.com/artifactory/webapp/#/home).

```
  npm i --save @cisco-ats/atomic-react
```

And in the target component add:

```
import ATextInput from "@cisco-ats/atomic-react/ATextInput";
```

## Usage

<Preview>
  <div className="atomic-ui-root--animated" style={{display:'flex',flexFlow:'column'}}>
    <p>
      <ATextInput placeholder="Normal" />
    </p>
    <p>
      <ATextInput placeholder="Disabled" disabled />
    </p>
    <p>
      <ATextInput placeholder="Read Only State" readOnly />
    </p>
    <p>
      <ATextInput validationState="warning" placeholder="Warning State" />
    </p>
    <p>
      <ATextInput validationState="danger" placeholder="Error State" />
    </p>
  </div>
</Preview>

## Text Input Props

<Props of={ATextInput} />


## Dusk Theme

<Preview className="theme--dusk" style={{backgroundColor: "#000", color: '#fff'}}>
<div style={{display: 'flex', flexFlow: 'column'}}>
  <ATextInput />
</div>
</Preview>

## Animations

Animations are enabled when a parent element has the `atomic-ui-root--animated` CSS class.

<Preview>
  <YourComponent>
{() => {
  const [checked, setChecked] = useState(false);
  return (
    <div className="atomic-ui-root--animated">
      <ATextInput />
    </div>
  );
}}
  </YourComponent>
</Preview>

---
name: Icons
route: /components/icon
components: AIcon
---

import {Playground, Props} from "../../../docs";

import AIcon from "./AIcon";
import Icons from "./icons.json";
import {AList, AListItem, AListItemAvatar, AListItemTitle} from "../AList";

# Icons

## Install

Integrate with your build to [auto-import](/#integrating), or add an import in your component:

```
import {AIcon} from "@threatgrid/atomic-react";
```

## Usage

<Playground
  code={`<>
  <AIcon className="status-yellow--text">warning</AIcon>
  <AIcon size={32} className="status-red--text">
    block
  </AIcon>
  <AIcon size="large">add</AIcon>
</>
`}
/>

## Props

The `AIcon` component inherits passed props.

<Props of="AIcon" />

## Gallery

<AList className="flex-row flex-wrap mb-8">
  {Object.keys(Icons).map((x) => (
    <AListItem
      name={x}
      key={x}
      className="flex-grow-0"
      style={{flexBasis: "33.33%"}}>
      <AListItemAvatar style={{minWidth: 24}}>
        <AIcon>{x}</AIcon>
      </AListItemAvatar>
      <AListItemTitle>{x}</AListItemTitle>
    </AListItem>
  ))}
</AList>

## Accessibility Notes

By default, `AIcon` components use the [aria-label](https://www.w3.org/TR/wai-aria/#aria-label) attribute to label the icon by name. This can be overridden by setting the `label` prop on the component.

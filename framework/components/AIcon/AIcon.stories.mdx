import { Meta, IconGallery, IconItem, Preview, Props } from '@storybook/addon-docs/blocks';

import AIcon from './AIcon';
import Icons from "./icons.json";

<Meta title="Components/Icons" component={AIcon} />

# Icons

## Install

The component is available on [Cisco's Artifactory package registry](https://devhub.cisco.com/artifactory/webapp/#/home).

```
  npm i --save @cisco-ats/atomic-react
```

And in the target component add:

```
  import AIcon from "@cisco-ats/atomic-react/AIcon"
```

## Usage

<Preview>
  <AIcon size="large">add</AIcon>
  <AIcon color="#dd2">warning</AIcon>
  <AIcon size="32" color="red">block</AIcon>
</Preview>

## Props

The `AIcon` component inherits passed props.

<Props of={AIcon} />

## Gallery

<IconGallery>
  {Object.keys(Icons).map(x => (
    <IconItem name={x} key={x}><AIcon>{x}</AIcon></IconItem>
  ))}
</IconGallery>

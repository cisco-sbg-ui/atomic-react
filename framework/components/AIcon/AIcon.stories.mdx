import {
  Meta,
  IconGallery,
  IconItem,
  Canvas,
  ArgsTable,
  Story
} from "@storybook/addon-docs/blocks";

import AIcon from "./AIcon";
import Icons from "./icons.json";

<Meta title="Components/Icons" component={AIcon} />

# Icons

## Install

Integrate with your build to [auto-import](?path=/docs/getting-started--page#integrating), or add an import in your component:

```
import AIcon from "@cisco-ats/atomic-react/AIcon"
// or if you're able to treekshake node_modules
import {AIcon} from "@cisco-ats/atomic-react";
```

## Usage

<Canvas>
  <Story name="usage-1">
    <>
      <AIcon className="status-yellow--text">warning</AIcon>
      <AIcon size={32} className="status-red--text">
        block
      </AIcon>
      <AIcon size="large">add</AIcon>
    </>
  </Story>
</Canvas>

## Props

The `AIcon` component inherits passed props.

<ArgsTable of={AIcon} />

## Gallery

<IconGallery>
  {Object.keys(Icons).map((x) => (
    <IconItem name={x} key={x}>
      <AIcon>{x}</AIcon>
    </IconItem>
  ))}
</IconGallery>

## Accessibility Notes

By default, `AIcon` components use the [aria-label](https://www.w3.org/TR/wai-aria/#aria-label) attribute to label the icon by name. This can be overridden by setting the `label` prop on the component.

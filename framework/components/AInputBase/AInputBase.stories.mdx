import {Meta, Preview, Story, Props} from "@storybook/addon-docs/blocks";
import {useState} from "react";

import AApp from "../AApp";
import AInputBase from "./AInputBase";

<Meta title="Extend/Input Base" component={AInputBase} />

# Input Base Component

## Install

In the target component add:

```
import AInputBase from "@cisco-ats/atomic-react/AInputBase";
// or if you're able to treekshake node_modules
import {AInputBase} from "@cisco-ats/atomic-react";
```

## Usage

<Preview>
  <Story name="usage-1">
    <AApp style={{maxWidth: 300}}>
      <AInputBase
        append="[Append]"
        prepend="[Prepend]"
        className="mb-3"
        clearable
        onClear={() => alert("cleared")}
        label="Label - Normal"
        hint="This is a sample of hint text below a form input">
        [Children]
      </AInputBase>
      <AInputBase
        append="[Append]"
        prepend="[Prepend]"
        className="mb-3"
        clearable
        onClear={() => alert("cleared")}
        label="Label - Focused"
        hint="This is a sample of hint text below a form input"
        focused>
        [Children]
      </AInputBase>
      <AInputBase
        append="[Append]"
        prepend="[Prepend]"
        className="mb-3"
        clearable
        onClear={() => alert("cleared")}
        label="Label - Read-only"
        hint="This is a sample of hint text below a form input"
        readOnly>
        [Children]
      </AInputBase>
      <AInputBase
        append="[Append]"
        prepend="[Prepend]"
        className="mb-3"
        clearable
        onClear={() => alert("cleared")}
        label="Label - Disabled"
        hint="This is a sample of hint text below a form input"
        disabled>
        [Children]
      </AInputBase>
      <AInputBase
        append="[Append]"
        prepend="[Prepend]"
        className="mb-3"
        clearable
        onClear={() => alert("cleared")}
        label="Label - Warning"
        hint="This is a sample of hint text below a form input"
        validationState="warning">
        [Children]
      </AInputBase>
      <AInputBase
        append="[Append]"
        prepend="[Prepend]"
        className="mb-3"
        clearable
        onClear={() => alert("cleared")}
        label="Label - Danger"
        hint="This is a sample of hint text below a form input"
        validationState="danger">
        [Children]
      </AInputBase>
    </AApp>
  </Story>
</Preview>

## Input Base Props

The `AInputBase` component inherits passed props.

<Props of={AInputBase} />

## Dusk Theme

<Preview style={{backgroundColor: "#18191d"}}>
  <Story name="dusk-1">
    <AApp defaultTheme="dusk" style={{maxWidth: 300}}>
      <AInputBase
        append="[Append]"
        prepend="[Prepend]"
        className="mb-3"
        clearable
        onClear={() => alert("cleared")}
        label="Label - Normal"
        hint="This is a sample of hint text below a form input">
        [Children]
      </AInputBase>
      <AInputBase
        append="[Append]"
        prepend="[Prepend]"
        className="mb-3"
        clearable
        onClear={() => alert("cleared")}
        label="Label - Focused"
        hint="This is a sample of hint text below a form input"
        focused>
        [Children]
      </AInputBase>
      <AInputBase
        append="[Append]"
        prepend="[Prepend]"
        className="mb-3"
        clearable
        onClear={() => alert("cleared")}
        label="Label - Read-only"
        hint="This is a sample of hint text below a form input"
        readOnly>
        [Children]
      </AInputBase>
      <AInputBase
        append="[Append]"
        prepend="[Prepend]"
        className="mb-3"
        clearable
        onClear={() => alert("cleared")}
        label="Label - Disabled"
        hint="This is a sample of hint text below a form input"
        disabled>
        [Children]
      </AInputBase>
      <AInputBase
        append="[Append]"
        prepend="[Prepend]"
        className="mb-3"
        clearable
        onClear={() => alert("cleared")}
        label="Label - Warning"
        hint="This is a sample of hint text below a form input"
        validationState="warning">
        [Children]
      </AInputBase>
      <AInputBase
        append="[Append]"
        prepend="[Prepend]"
        className="mb-3"
        clearable
        onClear={() => alert("cleared")}
        label="Label - Danger"
        hint="This is a sample of hint text below a form input"
        validationState="danger">
        [Children]
      </AInputBase>
    </AApp>
  </Story>
</Preview>
